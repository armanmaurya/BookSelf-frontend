import { RenderElementProps } from "slate-react";
import { TabPanel } from "../base/TabPanel";

export const RenderTabPanel = (props: RenderElementProps) => {
  return (
    <TabPanel {...props} />
  )
};
