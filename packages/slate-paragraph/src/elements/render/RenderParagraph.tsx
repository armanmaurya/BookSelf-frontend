import { ParagraphElementProps } from "../../types/element";
import BaseParagraph from "../base/BaseParagraph";

export const RenderParagraph = (props : ParagraphElementProps) => {
    return <BaseParagraph {...props}/>
}