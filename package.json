{
  "name": "bookself",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "apps/slate-editor"
    
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/armanmaurya/frontend.git"
  },
  "packageManager": "yarn@1.22.22",
  "scripts": {
    "dev": "turbo run dev",
    "dev:web": "start http://127.0.0.1:3000 && turbo run dev --filter=web --filter=@bookself/slate-paragraph",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "engines": {
    "node": ">=20.15.1"
  }
}
