{
  "name": "bookself",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "apps/slate-editor"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/armanmaurya/frontend.git"
  },
  "packageManager": "yarn@1.22.22",
  "scripts": {
    "dev": "turbo run dev",
    "dev:web": "turbo run dev --filter=web --filter=@bookself/slate-paragraph --filter=@bookself/slate-code",
    "build:web": "turbo run build --filter=web",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "engines": {
    "node": ">=20.15.1"
  },
  "dependencies": {
    "react-lottie-player": "^2.1.0"
  }
}
