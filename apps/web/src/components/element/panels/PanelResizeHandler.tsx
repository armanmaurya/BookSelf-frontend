"use client";
import { PanelResizeHandle, PanelResizeHandleProps } from 'react-resizable-panels'

export const ReactPanelResizeHandler = (props: PanelResizeHandleProps) => {
  return (
    <PanelResizeHandle {...props}/>
  )
}
